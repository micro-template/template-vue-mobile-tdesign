import {
  javascript, typescript, stylistic, vue, css, json, importX
} from "./config/index.js";

const DEFAULT = [
  javascript,
  typescript,
  stylistic,
  vue,
  css,
  ...json,
  ...importX
];

export default DEFAULT;
