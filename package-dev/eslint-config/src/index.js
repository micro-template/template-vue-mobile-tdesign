import {
  javascript, typescript, stylistic, vue, css, json
} from "./config/index.js";

const DEFAULT = [
  javascript,
  typescript,
  stylistic,
  vue,
  css,
  ...json
];

export default DEFAULT;
